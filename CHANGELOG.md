# CHANGELOG

> Texto integral de cada entrada: `git log -p -- CHANGELOG.md`.

## [Unreleased]

### Changed

- **`check-ceiling.sh` dissolved — each check moves to where its subject lives (2026-08-25)** — with the always-on ceiling gone, the file guarded nothing left in the kit. The provenance-narration check folds into `check-provenance.sh` (its own case-insensitive pass); the `$HOME/.../MEMORY.md` sub-check (a guaranteed CI `SKIP` since it was added) is dropped, not relocated. Gates go 4 → 3 in CI and `doctor.sh --maintainer`.

- **Always-on tier retired, and the apparatus it justified goes with it (2026-08-25) (core 0.30.0→0.31.0)** — `core:using-agent-kit` recorded 1 invocation across 927 startups and its SessionStart hook never executed (org policy blocks plugin hooks): unmeasurable content is retired rather than instrumented, its bugfix doctrine moved to the maintainer's own `CLAUDE.md`. Also retired: the skill-body injection, `check-ceiling.sh`'s byte ceiling, `emit-port.sh`/`install.sh --tool`/AGENTS.md emission, `OPERATIONS.md` §3, and the Retire-review paragraph.

- **`core:learn` deleted, `core:methodology` collapsed into its technical reference (2026-08-25)** — `learn` is superseded by the harness's own memory instructions; `methodology` keeps only the verification-gate section not tied to the retired tier, and promoting "Model vs. effort" to a real heading repairs three `core:prompt-optimizer` pointers that resolved to a bold bullet.

- **Fan-out ceiling, model pins, and a cap on the blind-review ritual (2026-08-09)** — `core:archaeology` stops hard-coding 4 Explore agents (a ceiling, not a target); `prompt-optimizer` gains the routing-is-not-fan-out clause; `grill-me` drops four `model: opus` pins for "session tier or above"; Council's blind-review ritual gains a cap so it can't generalize into a verification step. Opus 5 delegates more readily than prior generations, so this tightens a ceiling, not an incentive.

- **`model-routing` enters retire-review under Opus 5 (2026-08-09)** — the hook nudges toward delegation, the direction Opus 5 already over-fires; measurement only, no behavior change. Baseline is unreadable — `CLAUDE_PLUGIN_DATA` is unset, so markers land in a macOS-purged `$TMPDIR` — until that path is persistent.

- **House session default moves from Fable to Opus 5 (2026-08-09)** — `model-routing`'s tier tuple gains `opus` (renamed `SYNTHESIS_TIER_MARKERS`→`CONDUCTING_TIER_MARKERS`) so the conducting-tier-delegates-to-Sonnet rule stays mechanized under the new default; `prompt-optimizer` follows, naming Opus the session default and Fable the on-demand synthesis/critique tier.

- **`apk-archaeology`'s `report/` genre redefined internal-only — the contract that motivated it, not a relocation (2026-08-03) (mobile 0.16.0→0.17.0)** — the contract governing this class of engagement bars exposing decompilation origin to any channel where the client is present, and the skill still called `report/` "client-facing" in three places — the next run would have regenerated exactly the genre that had just been retired. `SKILL.md`, `method.md`, and `doc-standard.md` now call both `report/` genres internal-only; filenames and templates untouched.

- **`labs/` and `apks/` leave the kit; the `/wayfinder` era ends (2026-08-03)** — client-effort material moves to its own local, remoteless repo (`~/dev/labs`): one `.gitignore` line was all that separated 815 MB of client work, including a decompiled tree under NDA, from a public GitHub remote. The third-party `/wayfinder` skill is removed with it; its maps and 11 open tickets migrate to a new `issues/` tracker there.

- **Personal `MEMORY.md` byte ceiling raised 8192 → 12288 (2026-08-03)** — `check-ceiling.sh` §3's soft check kept forcing a trim pass on ordinary session-end writes; operator call to loosen it.

- **Governance v2: commit-time gate shrinks to provenance-only, the full gate moves to publish/CI (2026-08-02) (core 0.30.0, council 0.6.0, mobile 0.16.0)** — README generators, `INVENTORY.md`, the ceiling ratchet, `unwired/`, `validate_citations.py`, and `evals/` are all deleted; 7 hooks cut to 3; review falls back to manual citation spot-check. Always-on tier: 9595 → 6253 bytes.

### Added

- **`core:codebase-design` + `core:deepen-architecture` — a design vocabulary and the scan that speaks it, adapted from external skills (2026-08-02) (core 0.29.0)** — `codebase-design` is a corpus (deep-module glossary, principles, `DEEPENING.md`, `DESIGN-IT-TWICE.md`); `deepen-architecture` is one caller that scans for shallow modules and grills whichever the user picks. Split into two skills rather than one because a single-skill port would validate green and break on first invocation — the vocabulary is a deep module with many callers, not one caller's private reference. Adapted from `mattpocock/skills` (MIT), attributed in-body.

- **README pair gate closes its own coupling cost, `doctor.sh`/CI/`OPERATIONS.md` drift fixed, `docs/agents/` joins the gitignored working tree (2026-08-02)** — `README.pt-BR.md`'s two bash fences (the pair gate's own coupling surface) are replaced with pt-BR pointers into `README.md`, so the gate now passes vacuously instead of demanding byte-identical mirrors. `doctor.sh --maintainer` was silently only running five of seven documented gates; the missing two are wired in. CI gains the ceiling ratchet's own selftest.

- **README evolution: the diagram stops depending on GitHub's renderer, the routing section leads with a table, and the pair gets a gate (2026-07-30)** — the routing diagram moves from inline Mermaid (whose `look: handDrawn` rendering on GitHub was unverifiable) to a committed, `mmdc`-rendered light/dark SVG pair, so the claim is verified rather than delegated. The routing section now opens with a 9-row table naming each situation's observable discriminator. New `check-readme-pair.sh` gate found and fixed four real false-pass paths in itself, each pinned by a seeded negative case.

- **`apk-archaeology`'s `analysis/baseline.md` (G2) demotion carried through to source, full fix not surgical (2026-07-28; mobile 0.14.0→0.15.0)** — the one real run's `OVERVIEW.md` already performs G2's three functions without a dedicated `baseline.md` file, so the genre itself is demoted, not just the file: template deleted, `doc-standard.md`'s genre count corrected 3→2, `SKILL.md`'s run-layout line rewritten to the six files the run actually holds (two more phantom names, `feasibility.md`/`decisions.md`, dropped with it).

- **Worker output contract descends to `core:methodology` (2026-07-28; core 0.24.0→0.25.0)** — the always-on tier's §Dispatch carried the full 4-bullet Worker output contract inline; only the trigger line earns always-on space, so the bullets moved to `core:methodology` with a pointer left in their place. Ratchet lowered 10182→9811.

- **`check-provenance.sh` stops shipping the confidential literals it exists to ban — denylist split into structural (shipped) vs. literal (local, gitignored) tiers, operator decision (2026-07-28)** — the gate's `DENY` list held client/engagement literals in plain sight in a public, tracked file, including a comment that narrated the identity it was supposed to be hiding. Format-only patterns stay hardcoded; actual proper nouns move to a new gitignored `.provenance-deny`, with a tracked `.provenance-deny.example` documenting the format. Absence of the file (guaranteed on CI) runs structural-only, never fails hard.

- **Public-repo hygiene: a leaked client name in this file's own record, and the `check-provenance.sh` denylist gap that let it through (2026-07-28)** — an audit for the repo going public found two client-identifying leaks the gate hadn't caught (fixed to the kit's `telecorp` pseudonym) plus a stale `docs/OPERATIONS.md` claim about two skills' deletion state, corrected by citing the actual commit. The denylist gap that let both leaks through is closed with the two missing patterns.

- **12 native→Flutter migration lessons preserved to `unwired/flutter-migration-lessons.md` — seed for a not-yet-designed downstream skill (2026-07-27)** — a lab-side methodology doc (gitignored, would vanish with the lab) held 14 incidents from a real migration pipeline; 2 were already wired elsewhere, the remaining 12 are preserved here, translated and depersonalized. Deliberately not folded into `apk-archaeology` (different side of the migration boundary) and not promoted to wired (only one capability has completed all four phases so far).

- **`apk-archaeology` drops its Relational Fidelity Gate provenance blockquote and gains a third dynamic-capture instrument, `cdp_netcap.py` (2026-07-27; mobile 0.12.0→0.13.0)** — the gate's provenance blockquote duplicated a story `CHANGELOG.md` already owns and had tripped a false negative in `check-ceiling.sh`'s own provenance-narration grep; cut, with its live doctrine promoted to a real paragraph. New CDP network+console capture script promoted from a lab spike after closing four gaps (redaction reuse, fail-closed auth gate, argparse consistency, a real selftest) to match its siblings.

- **`scripts/check-ceiling.sh`'s provenance-narration grep — a literal-string match let a date-split phrase through (2026-07-27)** — the gate matched the exact string `Promoted from`, which a date-split phrase ("Promoted 2026-07-26 from...") did not contain, so it reported green over content it exists to ban. Fixed to a case-insensitive, date-tolerant regex, validated against every `promot*` occurrence in the shipped tree.

- **`team` follows `core:spec-refine` out (2026-07-27; team 0.3.0→0.3.1)** — `refine-async`/`refine-live` both named the retired skill; references removed. One rule (routing error paths/race conditions/complex state to `spec-refine`) now states the constraint with no owner, since no replacement was invented.

- **Hook timeouts, and a retirement marker on the three gates whose subject is a model behavior (2026-07-27; core 0.23.0→0.24.0)** — every `core` hook was inheriting Claude Code's 600s default; each now gets an explicit timeout sized to its measured typical, motivated by a hook once recorded at `durationMs: 901172, timedOut: true` (likely system sleep, not a live hang). New `# retire-review:` marker on the three hooks whose subject is a model behavior — re-verify necessity at each major model generation, since a stale block is a false positive, not just dead weight.

- **Record swept against the tree — the CHANGELOG's own stale claims (2026-07-27)** — a branch review found several sentences in this file were true when written and false by the branch tip (superseded claims, retired file locations, shifted line citations). Fixed here rather than after merge, since a self-contradicting record is the one defect a later commit can't cleanly undo. Rule going forward: cite the symbol, not the line number — line numbers rot on the next edit.

- **`core:methodology` regains "Analysis is a means, not a deliverable" (2026-07-27; core 0.22.0→0.23.0)** — retiring `core:pipeline` took its §5 Conduction rules with it; this restores exactly one of the original seven — the one telling whoever does the work what an analysis is *for*, regardless of who's conducting — rewritten to drop `pipeline`'s own retired vocabulary. Lands in tier 2 behind one new trigger phrase, not the always-on tier.

- **`citation-check.sh` — citation validation moves from skill-invoked to event-driven (2026-07-27; core 0.21.0→0.22.0)** — `validate_citations.py` used to run only when `grill-me`'s `pre-done` mode called it, which doesn't survive CE conducting the flow. New `PostToolUse(Write)` hook fires on any `*.findings.json` write, shells out to the validator with `--gate`, and blocks (exit 2) on an unverified citation — the flag's first real caller. Five review rounds fixed a false-positive substring match, silent fail-open notices that never reached the model, and a foreign-tool collision (any `findings.json` write anywhere triggered it); both `review-local` and `grill-me pre-done` are wired as producers. Evals grew 54→106 cases chasing each gap mutation testing exposed.

- **`no-silent-removal.sh` — the first always-on rule converted into a blocking mechanism (2026-07-26/27; core 0.20.0→0.21.0)** — the always-on "no silent removal of DI/observability annotations" rule was prose only; new `PreToolUse(Edit|MultiEdit)` hook blocks the edit unless a same-edit escape-valve comment justifies the drop. Five review rounds hardened the escape-valve regex (fail-open and fail-closed regressions on multi-marker strings), fixed a file-type gate that could self-lock the hook's own vocabulary, and closed a `MultiEdit`-shaped mutation-testing gap the suite had never exercised. Known limits (whole-file `Write`, imports, `@override`) stay declared, not fixed. Evals 54→83 cases.

- **`apk-archaeology` gains the Relational Fidelity Gate — `check_relational_claims.py` + doctrine (2026-07-26; mobile 0.11.0→0.12.0)** — promoted from a client migration's own methodology doc after its first version failed to catch a correction that left a stale claim on an anchor-sibling. New `method.md` section (two triggers, no skip-heuristic) plus a script that enumerates — never judges — relational-claim lexicon hits, anchor problems, and hand-typed numeric claims. Self-validated against a real `findings.yaml`: caught 5 residual anchor bugs plus one genuine line-range error the promoting session had missed.

- **`apk-archaeology` gains a documentation discipline — `references/doc-standard.md` + G1/G2 templates (2026-07-23; mobile 0.10.0→0.11.0)** — every run doc now follows a thin standard so output stops being a writer-structured data dump: three genres (G1 target-view, G2 baseline, G3 report), fact/inference/gap split into `§Observed`/`§Reading`/`§Open` with typed anchors, overwrite-not-append lifecycle. Convention-only this version, validated by a pilot retrofit of a real run.

- **`apk-archaeology` Foundation gains three promoted extractors — `compute_coupling.py`, `extract_layers.py`, `extract_permissions.py` (2026-07-22; mobile 0.9.0→0.10.0)** — cross-partition coupling, per-partition logical-layer composition, and raw declared manifest permissions (no domain/LGPD mapping — that stays a downstream judgment). `extract_permissions.py`'s tag regex was hardened before shipping: the original silently dropped any `<uses-permission>` tag carrying an extra attribute. New `digital-twin.md` recipe for an optional once-per-APK visual synthesis.

### Changed

- **`workflow-diagram-template.mmd` didn't render standalone — leading `%%` comments broke `mmdc`'s frontmatter detection (2026-07-30; core 0.28.0→0.28.1)** — `mmdc` requires the YAML frontmatter's opening `---` at line 1; four comment lines ahead of it broke detection entirely, producing a hard parse error with no SVG. Fixed by moving the comments after the closing `---`; the constraint is now stated explicitly in `technical-reference.md`.

- **Always-on tier tested against Anthropic's six context-engineering shifts for Claude 5 — one rule sharpened, two restatements cut, four shifts found already satisfied or inapplicable (2026-07-30; core 0.27.0→0.28.0)** — two shifts (progressive disclosure, specs-as-tests) are already ahead of the post's advice; the "stop repeating yourself" shift, the headline finding of a first pass, did not survive measurement — most apparent duplication carried payload the harness default couldn't supply. One genuine collision was settled by the operator: `Confirm understanding before producing` is renamed and narrowed to `Confirm the route before crossing plan → execution`. Net always-on: 9776→9595 bytes.

- **README gains a routing diagram and a pt-BR pair; the remaining cut comes from dedup, not from more deletion (2026-07-28)** — a 20% cut in text (2937→2344 words) came from collapsing three repeated facts (the `pr-review-toolkit` requirement, Council, the `mobile` cell) each to one location; the stated ≤160-line/≤14KB target was missed and is recorded here rather than dropped. `core:cold-reader` found and fixed six real gaps in the rendered page.

- **README loses its rationale essay, routing moves up next to Install (2026-07-28)** — the file argued at length *why* each seam sits where it does (30660→18924 bytes); a prior-art scan across five comparable READMEs settled a leaner target shape instead. Every load-bearing rule was mapped to a surviving location before the essay was deleted outright, not relocated.

- **Review round before publication — reviewer findings applied, three false claims corrected, one provenance leak closed (2026-07-28)** — four independent passes (two adversarial diffs, an intent review, a cold read) caught three README cuts that were substance not rationale, an overstated third-party claim, and a missing routing-table discriminator. A blind review separately caught a client name that reached this file and `docs/GOVERNANCE.md` via a `.provenance-deny` pattern that didn't match its own quoted form — the gate stayed green throughout; both passages now describe the discriminator generically.

- **`tech-breakdown` stops advertising a retired step (2026-07-28; core 0.26.0→0.27.0)** — the description promised "adversarial refinement" while step 5's own body already said the step was unowned/retired. Description drops the stale clause; step 5's heading gets `(no-op in this version)`.

- **`mobile`'s four hooks stop inheriting the 600s platform default (2026-07-28; mobile 0.13.0→0.14.0)** — none declared a `timeout`; each now gets an explicit value sized to what it costs. `di-mismatch.sh` gets the longest budget since it globs `lib/**` recursively.

- **Tier-2 thinning pass, and the kit's last attribution gap closed (2026-07-28; core 0.25.0→0.26.0)** — one `core:methodology` detail section was cut for merely restating its tier-1 Signal, with its orphaned tier-1 pointer removed to match. `prompt-optimizer` (vendored, MIT) gained the frontmatter half of the attribution convention it was missing — now the convention's only live example.

- **CE (`compound-engineering` 3.20.0, MIT, installed at user scope) adopted as flow conductor — agent-kit narrows to deterministic mechanism + the `mobile` vertical + `council` postures (2026-07-26/27, decision record)** — maintenance economy: a solo maintainer won't out-refine a large multi-contributor project on flow-conduction methodology, and CE is free (MIT). `superpowers` keeps discovery; CE conducts brainstorm/plan/execute/review/PR-feedback; agent-kit keeps hooks, scripts, gates, evals, `mobile`, `council`. An external analysis drove the decision but seven of its claims were falsified against this machine before the record could be trusted (CE ships zero hooks, no sunset rule exists, `review-local`/`tech-breakdown` stay wired) — none reproduced here uncorrected. Also settled: `core:learn`/`core:commit` stay as complements CE doesn't replace, the decision ledger repoints at CE's `session-settled:` records, and a NO-COMMIT exposure absent from the source analysis — six CE skills, including `ce-work`, are model-invocable and can commit/push/open a PR under the session's ambient permission mode, mitigated at user scope outside this repo.

- **`core:pipeline` retired, CE is the conductor (2026-07-26; core 0.19.0→0.20.0)** — the kit's last flow-conduction skill deleted now that CE conducts instead, plus every reference that would otherwise dangle (the always-on tier had already stopped citing it at `(core 0.17.0→0.18.0)`). Three §5 rules had no surviving copy at this commit (stage-order pull-gate, superpowers-coordination, the audience-in-planning input to `cold-reader`) — one is later restored elsewhere, the other two and the decision-ledger append rule stay gone by decision. The maintainer's own `~/.claude/CLAUDE.md` still named `core:pipeline` as entry point; fixed by him the same day.

- **Always-on tier absorbs the Worker output contract, stops citing `core:pipeline` (2026-07-26; core 0.17.0→0.18.0)** — prep for the pipeline deletion above: rather than drop the pointer and lose the content, the Worker output contract (verdict+evidence+file:line, STOP into PENDING DECISIONS, report build/test results) is inlined directly into the always-on tier, since `review-local` already depends on it as upstream infrastructure. Tier grew 9634→10128 bytes; ratchet raised to match — its first real exercise since being built.

- **`apk-archaeology` — `report/` capped at two client-facing genres, `analysis/` gains `transport.md` (2026-07-21; mobile 0.8.0→0.9.0)** — hardening where a real run exposed a hole: `report/` is now documented as exactly two on-demand genres (technical recon report, scope proposal), with no skill named as the proposal's generator since that tool was removed from the kit. `transport.md` becomes a first-class `analysis/` output for server-driven WebSocket/JSON-RPC channels, deliberately a doc and not yet a `migration_shape` schema bucket.

- **`learn` closes its own governance gap — deep dedup, merge-first, byte ceiling (2026-07-20; core 0.16.0→0.17.0)** — the skill that populates the memory index had no retirement/consolidation step, and its own ceiling check measured lines while the real gate measures bytes. Step 3's dedup now reads full file bodies instead of index one-liners and can propose a merge instead of always appending; the ceiling is now byte-based (~8192).

- **External-review restructure — cut surface, organize by domain (2026-07-20; core 0.15.0→0.16.0, mobile 0.7.0→0.8.0)** — applied an endorsed independent review: the dispatch-doctrine trio collapses to a pair (`pipeline` §6 + slimmed `methodology`), README cut ~23% by words and now opens on the worked example, and `apk-archaeology` is extracted from the `mobile` skill into `tools/apk-archaeology/` with the skill left as a thin conductor.

### Removed

- **`core:spec-refine` skill (2026-07-26; core 0.18.0→0.19.0)** — CE's `ce-brainstorm` runs the same gap interrogation and carries decisions into plan KTDs, so coverage isn't partial; deleted outright, not archived. Its only durable residue, the decision-ledger record format, moved into `grill-me`'s own references (later deleted when the ledger repointed at CE). Every dangling reference across the repo was cleaned; `tech-breakdown`'s step 5 loses its body and becomes an explicitly unowned disclosure sentence.

- **`core:orchestrate` skill** — loop machinery archived to `unwired/orchestrate/` (Saboo attribution intact); fan-out now defers to `superpowers:dispatching-parallel-agents`/native subagents. The reusable dispatch doctrine survived into `pipeline` §6.

### Added

- **`apk-archaeology` output contract mechanized — `findings.json` schema + `emit`/`render` scripts + `data/` topology (2026-07-18; mobile 0.5.0→0.6.0)** — the skill's output layer, previously hand-authored per run, gains a deterministic backbone with an honest sync boundary: scripts own only what's derivable from `data/*.json`, non-derivable fields are left null rather than fabricated. New `findings.schema.json`, `emit_findings.py`, `render_overview.py`/`render_c4.py`; `SKILL.md` pipeline rewritten to a `data/`+`decompile/` folder topology. Skill stays provisional.

- **`core:orchestrate` skill added — plan→delegate→verify→synthesize loop, adapted from `advisor-orchestrator-worker` (2026-07-16; core 0.14.0→0.15.0)** — an adapted port of `Shubhamsaboo/awesome-llm-apps` (Apache-2.0, attributed in-body), with four deliberate departures: advisor gates are propose-and-confirm not auto-fire, workers are the Agent tool not a CLI process, persistence uses the loop-manifest convention, and an explicit spend budget is enforced. `pipeline` proposes the loop when a route fans out into 3+ independent legs.

- **Meta LOOP — operational structure, Fase 0 (2026-07-16; core 0.13.0→0.14.0)** — doctrine-only instantiation of the kit's Orchestrator/Advisor/Worker topology so the kit runs the structure it already describes: `methodology` gains a Topology subsection and a `Type the work first` rule; `pipeline` becomes the stage-owner with a per-work-unit loop manifest, sanctioned where the earlier reverted global "hat" file `(core 0.8.0)` was not. The advisor-gate defaults to propose-and-confirm, not auto-fire, on operator token-cost concern. Acceptance deferred to a future substantial-work session.

- **Decision Ledger v1 — `spec-refine` emits, `grill-me`/`pipeline` consume (2026-07-15; core 0.11.0→0.12.0)** — mechanism mined from ACT Lite's Interview Ledger (MIT), extended with `Source` provenance typing and append-only `superseded(→L#)` status neither of which the original has. `spec-refine` step 5 now emits an `L#` record per resolved decision when the work has a spec on disk; `grill-me`'s `pre-done` mode requires every current record be covered or explicitly deferred. v1 scope deliberately narrow.

- **`grill-me` interview mode attributes its source; `core:pr` demoted to `unwired/` (2026-07-15; core 0.10.0→0.11.0)** — `grill-me` gains a one-line attribution to Matt Pocock's "grill me" prompt pattern. `core:pr` is commodity (native `gh pr create` already covers it) and moves to `unwired/`, unlike `core:commit` which stays wired for its lint+test+approval gate; every cross-reference across the repo is reworded to point at the native flow.

- **Multiagent-dispatch signal + doctrine (2026-07-14; core 0.9.0→0.10.0)** — `using-agent-kit` gains an always-on Dispatch section (the signal: fan-out/isolate/panel/session/don't-delegate shapes, proposal-only, never automatic); `methodology` gains the fuller on-demand doctrine (executor choice, dispatch/fan-in contracts, tool scoping, model routing).

- **`council:council` gains a "Scale of convening" paragraph (2026-07-14; council 0.5.0→0.5.1)** — one posture per decision is the default; a second is worn only when the Restate Gate reveals two genuinely distinct question-types inside the same decision, never to reinforce the first posture's verdict.

- **`prompt-optimizer` skill ported into `core` (2026-07-13; core 0.8.0→0.9.0)** — adapted from `LittleBearBond/everything-claude-code` (MIT, attributed in-body). The source's 6-phase pipeline is kept faithful; the component catalog, model recommendation, and lifecycle routing are re-pointed at this kit's real inventory, with no reference to the source ecosystem surviving the port. Advisory only — never executes the task itself.

- **Posture-signal gains two more signals — direction lock-in and past-discussion recall (2026-07-13; council 0.4.0→0.5.0)** — `proplasma` fires on lock-in language ("let's go with...") and points at `/core:grill-me pre-plan`; `anamnesis` fires on reaching for a past discussion and points at `/council:council-recall`. A post-ship blind review removed one branch as un-conservable against an open pt-BR word class; the resulting false negative is accepted and pinned by its own eval case. Evals 36→54.

- **Unwired triage — 17 items deleted by operator decision (2026-07-13)** — the operator reviewed the remainder of the unwired-items table and decided to delete rather than keep 17 items (hooks, scripts, two retired skills), each gone from the working tree and no longer eligible for promotion. `ui-comparison/` is the only survivor, kept dormant with a named promotion trigger.

- **`LICENSE` added (2026-07-13)** — MIT, Copyright (c) 2026 Arthur Avila, at the repo root.

- **Model-routing advisory lands in `core`; a same-day session-hat injection was built and reverted before it ever shipped (2026-07-13; core 0.7.1→0.8.0)** — new advisory `PreToolUse` hook nudges when the session model matches a synthesis-tier name (e.g. `fable`) writing a code artifact inline instead of delegating, riding `additionalContext` rather than blocking. A same-day feature injecting the operator's active "hat" from a hand-maintained state file was built, then reverted by operator contestation: a low-frequency hand-edited file is a near-certain staleness risk.

- **Proactive posture signaling — `council` gains its first hook (2026-07-13; council 0.3.0→0.4.0, core 0.7.0→0.7.1)** — new `UserPromptSubmit` hook pattern-matches the operator's prompt (pt-BR+EN) for six moments the kit already names and suggests the matching posture via `additionalContext`, never blocking; silence is the contract. Design de-bubbled against CHI 2025 research on user-initiative and structural contestation. An adversarial pass found and fixed five real gaps. Evals 23→32.

- **Vertical-identity refactor — Phase A (Prune) + Phase B (Refine), 2026-07-12** — operator-decided pivot: agent-kit becomes an epistemic-discipline kit for Flutter/Dart with `mobile` as flagship, replacing the prior generic framing. Phase A demotes 8 items to `unwired/`; the standalone citation gate is re-scoped as `grill-me`-internal infrastructure, not removed. Phase B refines three surviving conduction skills with no demotes. Phase C (2026-07-13) ships four previously-flagged mechanisms as real advisory hooks plus a promoted Lakos import-graph checker; versions bumped core 0.6.0→0.7.0, mobile 0.4.0→0.5.0.

- **Toolkit onboarding face + multi-tool port emitter (2026-07-11)** — `scripts/install.sh` (named profiles, `--dry-run`), `scripts/doctor.sh` (colleague checks by default, full gate behind `--maintainer`), and `scripts/emit-port.sh` (emits the always-on tier as `AGENTS.md` for Copilot/Cursor, generated into the consumer's repo, nothing committed back) — backed by a tier classification of all 35 skills into transpilable/coupled/enforcement-only. Supersedes the README's placeholder demo and comparison table from the Phase 5 entry below.

- **README rewrite around the epistemic-layer positioning (Phase 5, 2026-07-11)** — new tagline ("Superpowers decides what to do next; agent-kit decides whether to believe it"), a 3-layer architecture table, a stated public posture disclaiming any "proven" claim about model behavior, and a "which skill, when" map grouped by job. Per-plugin skill/hook/script counts removed from the plugin table as stale and already duplicated in `INVENTORY.md`.

- **Meta-governance layer removed from `docs/GOVERNANCE.md` (Phase 1 cut, 2026-07-10)** — D14/D16/D17 and the measurement meta-principle retired; `check-governance.sh` renamed `check-ceiling.sh`, now enforcing only the byte ceiling plus a scoped grep. `docs/GOVERNANCE.md` trimmed 157→55 lines. Accepted tradeoff: previously-provisional items lose their automated deadline gate and are kept by maintainer judgment instead.

- **Meta-governance cleanup — scripts & hooks (Phase 2 cut, 2026-07-10)** — removed the measurement apparatus (`analyze_tokens.py`, `census_usage.py`) and dead tooling; demoted 8 unused/orphan items to `unwired/`; removed the `require-core.sh` install-sanity hook from all three dependent plugins. Each removal paired at every layer (hooks.json, eval cases, fixtures, docs).

- **Note on the R2 A/B (§Aceite final (c), 2026-07-10)** — flagging for future readers: the behavioral A/B described in that section was a single exploratory pilot under session budget, not a conclusive test — its own text already names the likely confound. Surfaced here because the Phase 1 governance refactor dropped the ledger entry that used to point at this caveat.

- **Cold-reader define-done gate + audience-in-planning rule (2026-07-10)** — process change from a process audit: deliverable-producing work now names its audience in planning, and a deliverable whose audience didn't live the session gets a blind cold-reader pass at define-done. Wired as a new context-restricted agent (`Read`-only, receives only the rendered artifact + audience role), placed as an agent rather than a `grill-me` mode after operator challenge. Provisionally wired under the deadline-tracking mechanism, evidenced by a single ephemeral dogfood run.

- **English is now the default language for all authored kit content (2026-07-08)** — every skill, agent, hook comment, and manifest description across all 4 plugins is now in English; the former `grill-me`-only exception is gone. Runtime output still follows the thread's language, default English — deterministic scripts/hooks always emit English, model-generated output mirrors the prompt. Always-on tier real output: 10,018 bytes (was 10,879). A handful of schema/data values and target-language examples deliberately stay pt-BR.

### Changed

- **Tier-1 diet — pedagogy demoted to on-demand `methodology` (2026-07-15; core 0.12.0 → 0.13.0)** — the always-on `using-agent-kit` body shrank 10,476→8,529 bytes by moving each rule's Why/How-to-apply/Failure-mode down into `methodology`, leaving title+Signal+prohibition in tier-1 with a pointer. Nothing removed from the kit — verified bidirectionally, and a blind adversarial review confirmed all five 🔴 rules still self-fire from tier-1 alone. Token impact is marginal (~$1-2/mo); the real win is a leaner tier, not cost.

- **Versions (Phase 5, 2026-07-11)** — core 0.5.0→0.6.0, council 0.2.0→0.3.0, team 0.2.0→0.3.0, mobile 0.3.1→0.4.0, reflecting the Phase 1/2 meta-governance cut. Drift reconciled: mobile's manifest had silently moved to 0.3.1 with no matching entry — this bump's baseline is the actual value on disk.

- **Versions** — core 0.4.0 → 0.5.0, council 0.1.0 → 0.2.0, team 0.1.0 → 0.2.0, mobile 0.2.1 → 0.3.0, reflecting the English migration above.

- **D16 — governança de descriptions (Fase 3, 2026-07-08)** — regra de forma ("quando invocar"), tetos por classe (350 bytes; roteador 700) e agregado por plugin, com check 6 novo no `check-governance.sh`. Find-and-cut aplicado a 3 skills; `council` inteiro e 4 wired-provisórios ficam congelados até a decisão do censo.

- **Fase 2 da onda de refino — reestruturação em 4 plugins (2026-07-08, D15)** — `plugins/council/` e `plugins/team/` extraídos do `core` (move mecânico, zero reforma interna); fusão `advisor-check` → `grill-me` (modo entrevista + escalação); hook `require-core.sh` avisa quando `core@agent-kit` não está instalado. Versões: core 0.3.0, council 0.1.0, team 0.1.0, mobile 0.2.0.

- **Fase 1 da onda de refino pós-auditoria (2026-07-07)** — D17: registro machine-readable de provisórios em `docs/GOVERNANCE.md`, com check 5 (prazo vencido = gate vermelho) e marcador ⏳ no INVENTORY; critério slash-only escrito no `SKILL-CONTRACT.md`; CI passa a rodar os 3 gates + evals tier-1 + shellcheck em push/PR.

- **Governança consolidada em `docs/GOVERNANCE.md` (Marco 2, 2026-07-07)** — doc canônico único com o modelo de 3 estados (D10), regra de promoção, teto do tier sempre-ativo e ledger de decisões resolvendo todo ID `D*`/`R*` citado no repo. Novo gate `check-governance.sh` falha se algum ID citado não resolver no ledger. Sempre-ativo enxugou ~1KB.

- **Conselho de Posturas completo promovido a wired (2026-07-07)** — `bohr`, `sagan`, `council`, `council-log`, `council-recall`, `maxwell`, `zeno`, `epistemic-council` promovidos sob a exceção de deadline de rotação, decidida pelo dono após auditoria de harness. Prazo de validação: sem uso real em 1 ciclo, volta a `unwired/`. Review adversarial pós-promoção corrigiu descriptions e convenções de namespace.

- **Exceção de promoção por deadline de rotação codificada em `unwired/README.md`** (regra completa lá; `using-agent-kit` §Governança ganhou o ponteiro).

- **Hardening do `bash-autoapprove-readonly.sh`** (finding da auditoria 2026-07-07) — `git fetch`, criação de branch/tag, `pub get` e `flutter/dart test` saem da classificação read-only e passam a deferir, por mutarem lockfile/rede/executarem código. +7 eval cases (30 no total).
- Versões nos manifests (`core` 0.2.0, `mobile` 0.1.0) e descrição na marketplace.

### Alterado

- **Versões (Fase 3, 2026-07-08)** — core 0.3.0→0.4.0 (referência model-vs-effort, gatilho no `compound`); mobile 0.2.0→0.2.1 (descriptions cortadas pro teto D16). Tag `v0.4.0` criada local após review adversarial.

- **Gate de proveniência exclui `.worktrees/` (Fase 3, 2026-07-08)** — o path absoluto embutido no ponteiro `.git` de um worktree casava a denylist por substring, deixando o gate vermelho em árvore local; `.worktrees/` é workspace gitignored e nunca alcança o CI.

- **Gate de governança exclui `.worktrees/` do censo de IDs (Fase 3, 2026-07-08)** — simétrico ao fix de proveniência: o censo de IDs `D*`/`R*` varria worktrees isolados de outros branches, deixando o gate vermelho em árvore local com worktree registrado.

- **README no modelo ACT Lite (Fase 3, 2026-07-08)** — voz de produto direta: tagline → Por que este kit → O que está incluído → Instalação → Workflow → Requisitos → Avançado. Sai a abertura-manifesto e o glossário (termos internos passam a ser explicados inline).

- **Docs 4→2 (Fase 3, 2026-07-08)** — `docs/GOVERNANCE.md` absorveu `docs/SKILL-CONTRACT.md` e `docs/OPERATIONS.md` absorveu `unwired/README.md`; parsers atualizados pro novo local.

- D14 §Escopo: correção cirúrgica (linhas pontuais, sem mudar esqueleto/propósito) não constitui reforma nem obriga conformidade — decisão do dono, 2026-07-07.
- `review-local` (fix cirúrgico): mandato de tool Read para evidência citável no dispatch + claim do read-ledger anotada com o veredito do teste de runtime de 2026-07-07 (reads de subagent via Read entram no ledger da sessão-mãe; via Bash não).
- `using-agent-kit` §Vocabulário de Posturas: as 6 posturas agora são todas wired (4 skills in-thread + 2 subagents) com ponteiro pro índice `core:council`.
- README: contagens atualizadas (core: 25 skills, 4 agents), tabela de agents, seção unwired, descrição do autoapprove.
- Auditoria de harness & identidade (2026-07-07, workflow de 15 agentes): relatório completo fora do repo (contém contexto do projeto de origem — o gate de proveniência recusa, corretamente) em `~/.claude/audits/2026-07-07-harness-identity-audit.md`.

- Skeleton inicial do repo com plugins core e mobile
- README.md completo (instalação, mapa wired dos 2 plugins, unwired/promoção (à época archive/), assets manuais, princípios, nota de double-loading)
- `core:pipeline` — condutor de fluxo (camada de routing conversacional): detecção de estágio, 5 classes de tarefa com rotas, tabela estágio→skill com fallbacks sem superpowers, disciplina de sessão. Spec: `docs/superpowers/specs/2026-07-06-conversational-flows-pipeline-design.md` (resíduo de construção removido da superfície em 2026-07-07 — recuperável na história do git)
- Promovidos de `unwired/` para wired: `bug-report`, `refine-live`, `refine-async` (`plugins/core/skills/`) e `figma-to-component` (`plugins/mobile/skills/` — Flutter-only, não cabia em `core`). `bug-report` já era skill-folder válida (só mover + remover nota de proveniência); `figma-to-component` teve a `description` reescrita e ganhou `disable-model-invocation: true`; `refine-live`/`refine-async` converteram de comando flat pra skill-folder com frontmatter (mesmo padrão usado em `tech-breakdown`/`archaeology`/`advisor-check` na extração original). Decisão de promoção — exceção deliberada à regra padrão de "uso real comprovado", motivada pelo prazo de desalocação do workspace de origem — documentada num plano de sessão externo a este repo (não versionado aqui). Revisão pós-promoção (xhigh, workflow de code-review) encontrou 15 defeitos de conteúdo introduzidos pela promoção mecânica — referências cruzadas mortas, placeholders não preenchidos, nomes de tool inexistentes no Claude Code, convenção `/core:`/`/mobile:` não aplicada no corpo das skills — todos corrigidos na mesma sessão.

### Aceite final (spec §7) — via marketplace local

Rodado em dois projetos virgens sob `$TMPDIR` (`git init` + `claude plugin marketplace add "$HOME/dev/agent-kit"`), sem depender de GitHub.

**(a) Projeto Flutter virgem, core+mobile instalados (`-s project`):**
- `using-agent-kit` presente: hook `session-start.sh` injeta o corpo da skill (confirmado via `claude -p`, sessão relata regras "Grep antes de responder convenção" e "Evidência antes de claim").
- `core:*` e `mobile:*` invocáveis: 9/9 skills mobile listadas e coerentes com o disco; 7/16 skills core aparecem na auto-enumeração do modelo — as 9 restantes (`advisor-check`, `archaeology`, `commit`, `compound`, `pr`, `review-local`, `review-remote`, `spec-refine`, `tech-breakdown`) têm `disable-model-invocation: true` no frontmatter (funcionam só via `/core:<nome>` explícito, não pela tool Skill) — confirmado invocando `/core:archaeology` diretamente, respondeu corretamente.
- `learn` gravando: verificado à parte, num terceiro projeto virgem (`$TMPDIR/learn-check`, `git init` + mesmo marketplace local), via `claude -p "Usando a skill core:learn: eu prefiro sempre usar tabs em vez de espaços. Capture essa preferência como uma proposta de memória — mostre o arquivo de memória que você proporia (frontmatter + corpo)."`. Resultado observado: a skill carregou, procurou `MEMORY.md` (ausente, sem duplicata), e devolveu uma proposta estruturada — frontmatter `name`/`description`/`type: feedback` + corpo com `Why`/`How to apply` + entrada correspondente pro índice `MEMORY.md` — parando explicitamente para aprovação (`approve`/`edit ...`/`skip`) sem gravar nada em disco (confirmado: diretório do projeto ficou só com `README.md` do `git init`, nenhum `.md` novo). Isso é o comportamento correto do fluxo propose-then-approve da skill, não uma falha. **PASS.**
- Hook dispara: `session-start.sh` (core) e `smell-checker.sh` (mobile, bloqueou DI001 com exit 2) rodados direto no cache instalado (`~/.claude/plugins/cache/agent-kit/{core,mobile}/66a0edfe53fe`) — ambos confirmados fora do harness de eval.
- `.mcp.json` registrado: `claude mcp list` mostra `plugin:mobile:dart` e `plugin:mobile:marionette`, ambos ✔ Connected (binários presentes no PATH da máquina de teste).
- Zero ref de proveniência quebrada: denylist de `check-provenance.sh` (à época `check-no-tf.sh`) rodada contra o cache instalado (`plugins/cache/agent-kit/{core,mobile}/66a0edfe53fe`) — limpo.

**(b) Projeto não-mobile virgem, só core instalado:** confirmado por `claude -p` que não há nenhuma skill `mobile:*` disponível e nenhum MCP `dart`/`marionette` — e que o contexto `using-agent-kit` injeta normalmente (mesma citação de regra 🔴 que em (a)).

**(c) R2 — custo de payload + A/B comportamental:**
- `analyze_tokens.py`: achado colateral — o script resolve plugin instalado em múltiplos scopes pegando `installs[0]` de `installed_plugins.json`, não necessariamente o scope efetivo da sessão; nesta máquina havia um `core@agent-kit` (scope user) preso num commit anterior ao gate-dia-3 (`4aabfbc`, o commit imediatamente anterior ao commit tageado `gate-day3-pass` = `1674a57`), medindo 15 skills em vez de 16 — corrigido com `claude plugin update core@agent-kit` antes de medir (ação fora do repo, disclosed aqui).
  - Core+mobile juntos (projeto a): **2.321 tokens/turno** medidos (chars÷4, sem tiktoken instalado) — agent defs (mobile 990 + core 526) + skill listings (core 499 + mobile 306). On-demand (custo só quando skill roda): 31.856 tokens.
  - Só core (projeto b): **1.025 tokens/turno**. On-demand: 22.703 tokens.
  - Lacuna do script: `analyze_session_hooks()` só detecta arquivo estático `session-start.md`/`STARTUP.md`; não mede a saída dinâmica de `hooks/session-start.sh`, que injeta o corpo inteiro de `using-agent-kit` uma vez por sessão. Medido à parte via `wc -c` na saída real do hook: **11.737 chars ≈ 2.934 tokens** (mesma aproximação chars÷4) — custo de sessão, não por turno, ausente do relatório do script pra esse padrão de hook.
  - **Total realista da 1ª mensagem de uma sessão**: ~5.255 tokens (core+mobile) ou ~3.959 tokens (só core), além do baseline fixo do Claude Code (~9k, não relacionado ao kit).
- A/B comportamental: tarefa idêntica ("qual biblioteca de logging esse projeto usa?" contra um arquivo com `structlog`) rodada com e sem `core@agent-kit` habilitado no mesmo projeto (b). **Sem diferença observável** — as duas rodadas leram o arquivo antes de responder e acertaram (`num_turns=5` em ambas). Hipótese mais provável: o ambiente de teste já tinha `superpowers` instalado (outro plugin com disciplina epistêmica sobreposta) e/ou o modelo (Sonnet 5) já exibe esse comportamento de baseline sem o rule textual explícito — o teste não discrimina nesse cenário específico. Procedimento documentado para o dono rodar uma bateria maior (2-3 tarefas, projeto realmente limpo sem `superpowers`) se quiser sinal mais forte; só 1 rodada A/B foi executada de fato aqui (não só documentada) por orçamento de tempo/custo da sessão.

### Métricas D13

- gate-day3: 2026-07-06 14:12:26 -0300 (commit `1674a57`, tag `gate-day3-pass`)
- métrica-2-semanas: primeiro uso real em projeto fora do projeto de origem até 2026-07-20 — senão foi "inventário com README"
