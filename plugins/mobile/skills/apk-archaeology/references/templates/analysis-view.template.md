<!-- G1 target-view — copy per analysis/ view. Discipline: references/doc-standard.md §G1. -->
# <View name> — <one-line scope>

**Reader:** engineer · **Use:** <what they decide/do with this view> · **Source:** <mapping rule, e.g. "data/endpoints.json + grep over decompile/jadx/">

## Observed
<!-- Facts only. Each claim carries a typed anchor: `path/File.java:123` (static) OR `capture:<id>` (dynamic). No anchor → move to Reading or Open. Do NOT repeat 🟢 here — the section is the signal. -->
- <fact> — `<file:line | capture:id>`

## Reading
<!-- Analyst inference. Each line anchored to an Observed fact it rests on. -->
- <inference> (rests on: <which Observed fact>)

## Open
<!-- Named gaps, information-request style. Never silently filled. -->
- <what is unknown, and what evidence would close it>
